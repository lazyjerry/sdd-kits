# Step 04 - 驗證提案格式（Validate）

> 提案建立後執行驗證，確保格式正確。
> 避免歸檔時才發現規格合併失敗。
> add-user-search 是該次變更的名稱

```bash
openspec validate add-user-search --strict
```

驗證失敗時，用以下指令找出問題：

```bash
openspec show add-user-search --json --deltas-only
```
