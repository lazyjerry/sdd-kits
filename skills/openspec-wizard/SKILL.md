---
name: openspec-wizard
description: >-
  OpenSpec SDD 操作指引精靈。當使用者意圖使用 OpenSpec、提及 SDD 開發並選擇 OpenSpec、
  或要求以規格驅動開發方式建立提案時觸發。
  以選單方式呈現所有步驟，引導使用者完成從初始化到歸檔的完整流程，
  並將每步操作記錄保存為 log 檔案。
---

# OpenSpec 操作指引精靈

引導使用者完成 OpenSpec SDD 工作流程：從安裝、初始化到提案、實作、歸檔。

## 啟動流程

### 1. 環境檢查

執行以下終端指令確認 OpenSpec 是否已安裝：

```bash
openspec --version
```

- **已安裝** → 顯示版本號，進入步驟選單
- **未安裝** → 顯示安裝指引後停止：

```
⚠️ 尚未偵測到 OpenSpec CLI。請先安裝：

  npm install -g @fission-ai/openspec@latest

需要 Node.js 20.19.0 以上版本。
安裝完成後請重新啟動此精靈。
```

### 2. 詢問工作目錄

確認使用者的目標專案目錄：

```
請問要在哪個專案目錄執行 OpenSpec？
（輸入路徑，或按 Enter 使用目前工作區）
```

### 3. 顯示步驟選單

```
══════════════════════════════════════
  OpenSpec 操作指引精靈
══════════════════════════════════════

請選擇要執行的步驟：

  [0] 初始化專案        — openspec init
  [1] 填寫專案資訊      — 編輯 openspec/project.md
  [2] 建立變更提案      — /opsx:propose <描述>
  [3] 檢查規格格式      — 檢查 SHALL/MUST + Scenario
  [4] 驗證提案          — openspec validate <name> --strict
  [5] 開始實作          — /opsx:apply
  [6] 歸檔              — /opsx:archive
  ─────────────────────
  [A] 進階：design.md 與多 Specs
  [C] 常用指令速查
  [Q] 結束精靈

輸入編號繼續：
```

### 4. 執行步驟

讀取 `<SKILL_DIR>/references/steps.md` 取得選定步驟的完整指引，依照該步驟：

1. **顯示步驟說明**：該步驟的目的與注意事項
2. **提供提示詞模板**：可直接使用的指令或提示
3. **引導使用者輸入**：收集必要資訊（如提案描述、變更名稱等）
4. **協助執行**：根據步驟類型，執行 CLI 指令或提供 AI 提示詞
5. **記錄到 log**：將使用者輸入與執行結果寫入 log 檔案

### 5. 步驟完成後

- 更新 log 檔案
- 顯示下一步建議
- 回到步驟選單

## Log 機制

### Log 檔案位置與命名

```
<專案根目錄>/sdd-logs/openspec-<YYYY-MM-DD>-<提案名稱>.md
```

- 首次啟動時建立 `sdd-logs/` 目錄（如不存在）
- 提案名稱在 Step 2 建立提案時確定；Step 0～1 使用 `init` 作為暫時名稱
- 同一提案的所有步驟記錄在同一檔案中

### Log 檔案初始化模板

首次建立時寫入：

```markdown
# OpenSpec Log: <提案名稱>

- **工具**：OpenSpec
- **日期**：<YYYY-MM-DD>
- **專案**：<專案路徑>

---
```

### 每步驟追加格式

每完成一個步驟，append 以下區塊：

```markdown
## Step <N>: <步驟名稱>

**時間**：<HH:MM>

### 使用者輸入

<使用者提供的原始輸入或選項>

### 執行內容

<實際執行的指令或提示詞>

### 執行結果

<AI 輸出摘要或 CLI 輸出>

### 狀態

✅ 完成

---
```

## 關鍵規則

| # | 規則 | 說明 |
|---|------|------|
| 1 | 先讀再做 | 每次執行步驟前，先讀取 `references/steps.md` 對應段落 |
| 2 | 忠實記錄 | 使用者的每次輸入都必須記錄到 log |
| 3 | 不跳步驟 | 選單讓使用者自由選擇，但提醒建議順序 |
| 4 | 範圍控制 | 實作階段提醒使用者「請專注在 tasks.md 列出的項目」 |
| 5 | 使用新版指令 | slash command 使用 `/opsx:*` 格式 |
| 6 | 錯誤處理 | CLI 指令失敗時，顯示錯誤訊息並提供修正建議 |
