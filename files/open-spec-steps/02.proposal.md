# Step 02 - 建立變更提案（Proposal）

> 新增功能、破壞性變更、改動架構時，先建立提案。
> 系統會產生 `proposal.md`（原因與範圍）、`tasks.md`（待辦清單）、`specs/`（規格差異）。

> 除了 command 之外，或是使用 skill 的對話方法請求建立 proposal
```
/openspec:proposal 新增使用者搜尋功能
```
